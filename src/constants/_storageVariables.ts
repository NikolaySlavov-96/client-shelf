const STORAGE_PREFIX = '@Product_';

export const STORAGE_KEYS = {
    TOKEN_DATE: `${STORAGE_PREFIX}TokenData`,
    USER_DATA: `${STORAGE_PREFIX}UserData`,
    ISSUE_ROOMS: `${STORAGE_PREFIX}issueRooms`,
    CLIENT_ID: `${STORAGE_PREFIX}ClientId`,
};
